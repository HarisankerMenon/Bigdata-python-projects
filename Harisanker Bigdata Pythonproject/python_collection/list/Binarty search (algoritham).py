#Binarty search (algoritham)

#:- to reduse time of leniar search


#   1) sort the array in (aesc order)
#   2) declare 2 variables
         # low=0   ( first position )
         # upper= length of lst -1 (last position)

#   3) calculate mid

       # syntax :-
                   '''mid=(low+upp)//2'''

#   4) check 3 condition

#          a) search_element > list(mid)

#               :- if search element > lst(mid)
#               :- set low as = mid+1

#          b) search_element < list(mid)

#               :- upper=mid-1  (set upper as)

#          c) seach_element == list(mid)
#
#                    element found
#
#
#
